# 1. Práctica <!-- omit in toc -->

# 1. Crear un nuevo repositorio en Github
- Nombre: `test-page`


# 2. En la terminal ir a `/home/azureuser`
```
cd /home/azureuser
```

# 3. Crear y entrar en la carpeta `test-page`
```
mkdir test-page
cd test-page
```

# 4. Sincronizar con el nuevo repositorio `test-page`

# 5. Crear un `index.html` en la carpeta test-page

La página debe incluir un título: "Prueba de CI/CD en AzureDevops"
Y el nombre del estudiante.

Opcional, pude usar el siguiente prompt para construir la página con un LLM.
```
You are a frontend developer. Create a simple landing page with the following features:
- A title that says: "Prueba de CI/CD en AzureDevops"
- A subtitle that says: "by <STUDENT_NAME>"
- The page should be responsive and work on both desktop and mobile devices
- The page should be visually appealing and modern
- Style: colorfull, use gradients and modern design principles, add a background smooth patern and a blur northern lights effect, use CSS
```

# 6. Crear un Dockerfile para la página web
- Incluir el `index.html` del punto anterior
- Investigar como ejecutar el `index.html` dentro de un contenedor utilizando un servidor estático como `nginx` o `http-server`.

# 7. Sincronizar el proyecto con Github
# 8. Crear un nuevo Azure Container Registry repository para la nueva app
# 9. Crear un `pipeline` para nuevo proyecto con las siguientes características
- Construir la imagen utilizando el archivo dockerfile del proyecto
- Trigger al branch: main
- Subir la imagen a Azure Container Registry

# 10. Sincronizar el proyecto con Github y validar la ejecución del `pipeline`

# 11. Ejecutar el contenedor en la máquina virtual usando la imagen del ACR
- Puerto 3001
- Nombre test-page

# 12. Probar en browser
Usando la IP pública de la máquina virtual y el puerto `3001`

# 13. CHECKPOINT: Anotar evidencia de la ejecución del contenedor en puerto 3001


