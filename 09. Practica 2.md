# 09. Practica de Despliegue <!-- omit in toc -->

# 1. Crear un Azure Container App para la aplicación `test-app`
# 2. Crear el pipeline de despliegue para la aplicación `test-app`
	- pool: local-agent
	- Conectar con el nuevo Azure Container Apps: test-app
	- Crear un nuevo Service Connection y usarlo en el pipeline
# 3. Agregar un archivo `TEST-APP.md` al proyecto y anotar el `Application URL`.
# 4. Sincronizar con Github.
# 5. Validar la ejecución del pipeline y validar que la página se muestre correctamente usando el `Application URL`.


