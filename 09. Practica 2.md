# 09. Practica de Despliegue <!-- omit in toc -->

# 1. Crear un Azure Container App para la aplicación `test-page`
# 2. Crear el pipeline de despliegue para la aplicación `test-page`
	- pool: local-agent
	- Conectar con el nuevo Azure Container Apps: test-page
	- Crear un nuevo Service Connection y usarlo en el pipeline
# 3. Agregar un archivo `TEST.md` al proyecto y anotar el `Application URL`.
# 4. Sincronizar con Github.
# 5. Validar la ejecución del pipeline y validar que la página se muestre correctamente usando el `Application URL`.
# 13. CHECKPOINT: Anotar evidencia de la ejecución de la app usando el `Application URL`.

