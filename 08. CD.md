# 08. Despliegue Continuo <!-- omit in toc -->

# 1. Instalar AZ CLI en la VM
```
curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash
```

# 2. Crear en Azure: Container Apps

- Basics
  - Nombre: app-demo-page-[ESTUDIANTE]
  - Publish Container
  - Region: East US 2
  - Container Source: Container Image
  - continue

- Container
  - Registry
  - Image
  - Tag (el último)
  - Workload profile
    - 0.25 CPU - 0.5Gi memory
  - continue

- Ingress:
  - Accepting Traffic from Anywhere
  - Insecure connections
  - Target port: 3000
- Crear

# 3. En Azure DevOPs:  Crear un service connection
- Project settings:

- Tipo: Azure Resource Manager
- Método: App Registration (automatic)
- Seleccionar Suscripción y RG
- Nombre: appDeployConnection
- Grant access permission to all pipelines


# 4. Crear el deploy en yml
## 4.1. task de prueba
```yaml
      - job: Deploy
        displayName: Deploy
        dependsOn: Build
        pool: local-agent
        steps:
          - script: echo "Deploying..."
```

## 4.2. Buscar credenciales de ACR
- Settings - Access Keys

## 4.3. Crear las credenciales de ACR como secrets

- acrUsername
- acrPassword

## 4.4. Agregar el deploy del Container App
> [Azure web apps](https://learn.microsoft.com/en-us/azure/devops/pipelines/tasks/reference/azure-container-apps-v1?view=azure-pipelines)
```yaml
      - job: Deploy
        displayName: Deploy
        dependsOn: Build
        pool: local-agent
        steps:
          - task: AzureContainerApps@1

            inputs:
              azureSubscription: 'appDeployConnection'
              imageToDeploy: acrapps<ESTUDIANTE>.azurecr.io/${{ parameters.imageName }}:${{ parameters.tag }}

              containerAppName: 'app-demo-page-[ESTUDIANTE]' # NOMBRE DEL CONTAINER APP
              resourceGroup: <RG_NAME>

              acrUsername: $(acrUsername)
              acrPassword: $(acrPassword)
```

> Best Practice: agregar las credenciales como SECRETS.

# 5. Ejecutar el pipeline
- Dar permisos si es necesario

## 5.1. Validar todos los steps correctos

# 6. Revisar el Container App
- Application - Revision and Replicas

# 7. Validar en Browser
# 8. Hacer cambio de version
- Cambiar la variable en Azure DevOps: `appVersion`
- En el archivo `.env` cambiar la variable `BG_COLOR`
- Sincronizar con Github.

## Opcional: Hacer la versión dinámica
```yaml
            arguments: '--build-arg VERSION=${{ parameters.tag }}'
```

# 9. Validar logs
- Monitoring - Log Stream

# 10. Opcional. Configurar un ambiente
Un Environment (Ambiente) en Azure DevOps Pipelines un ambiente representa el destino físico o lógico donde se despliega tu aplicación (por ejemplo: development, staging, production).

- Pipelines - environments
- Name: Deploy-vm
- Type: Virtual Machine
- Copiar el "Registration script"

## 10.1. En la VM
- Ejecutar el "Registration script"
- server URL: https://dev.azure.com/<my-organization-name>
- PAT: xxxx


## 10.2. Ejemplo de uso del ambiente
```yaml
stages:
- stage: deploy_staging
  jobs:
  - deployment: DeployApp
    environment: 'staging'
```













